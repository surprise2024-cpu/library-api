import type { Author } from "../models/author.js";
import type { Book } from "../models/books.js";

// authors must be an array containing objects that match the Author object
export const authors: Author[] = [];

// books must be an array containing objects that match the Book object
export const books: Book[] = [];